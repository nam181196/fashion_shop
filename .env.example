### Example environment variables for local development
# Copy this file to `.env` and fill values before running `docker-compose up`

# Database connection used by the app (inside docker-compose)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=bankrate_db
DB_USERNAME=bankrate
DB_PASSWORD=password

# Optional: override application server port
# SERVER_PORT=8080
